# OLORIC Model Dependencies
# Targeted for NVIDIA L4 24GB (Ada Lovelace, sm_89) on Lightning AI Studio
# Minimum Python version: 3.10+ (recommended: Python 3.10 or 3.11)

# PyTorch
# In Lightning AI Studio, CUDA-enabled PyTorch is pre-installed.
# Do NOT install CPU-only wheels on GPU studios.
torch>=2.1.0

# Hugging Face Core Stack
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.28.0
datasets>=2.18.0

# 4-bit Quantization / QLoRA
bitsandbytes>=0.43.0

# Data Validation & Schemas
pydantic>=2.0.0

# Configuration & Utilities
PyYAML>=6.0
numpy>=1.24.0

# Optional / Testing
pytest>=7.0.0
